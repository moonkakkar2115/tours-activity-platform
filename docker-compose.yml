version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: tours-activity-platform_mongo_1
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  backend:
    build: ./backend
    container_name: tours-backend
    ports:
      - "5000:5000"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017/toursdb

volumes:
  mongo-data:

  frontend:
    build: ./frontend
    container_name: tours-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - tours-network

networks:
  tours-network:
    driver: bridge
volumes:
    mongo-data:
